/**
 * @author prabha
 */
public class Set {
    public static void main(String[] args) {

        //The set does not allow duplicate elements.
        //The set does not maintain any insertion order.
        //But in the set almost only one null value.
        //Set implementation classes are HashSet, LinkedHashSet, and TreeSet.
        //HashSet allows NULL Elements
        //Objects that you insert in HashSet are not guaranteed to be inserted in the same order.
        // Objects are inserted based on their hash code. LinkedHashSet can be used to maintain order
        //HashSet is not Synchronized but it can be synchronized externally
    }
}
